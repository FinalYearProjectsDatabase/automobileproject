<?php

    // define global variables to access them anywhere in the system
    define('HOSTNAME', '127.0.0.1');

    define('USERNAME', 'root');

    define('PASSWORD', '');

    // define('DATABASE', 'fix_automobile');

    define('DATABASE', 'autofixproject');

    define('DSN', 'mysql:host='.HOSTNAME.';dbname='.DATABASE);